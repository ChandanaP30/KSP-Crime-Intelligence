/* ============================================================================
   CRIME INTELLIGENCE DASHBOARD - PREMIUM ENTERPRISE REDESIGN
   Inspired by: Palantir Gotham, ArcGIS Intelligence, IBM i2 Analyst's Notebook
   ============================================================================ */

.app-shell {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: var(--bg);
  overflow: hidden;
}

/* ============================================================================
   HEADER - PREMIUM BRANDING
   ============================================================================ */

.top-bar {
  padding: var(--spacing-xl) var(--spacing-2xl);
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: flex-start;
  gap: var(--spacing-xl);
  background: linear-gradient(180deg, var(--surface) 0%, rgba(15, 21, 39, 0.95) 100%);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
  position: sticky;
  top: 0;
  z-index: var(--z-sticky);
}

.top-bar h1 {
  font-size: 32px;
  font-weight: 800;
  letter-spacing: -1px;
  margin: 0;
  background: linear-gradient(135deg, var(--text-primary) 0%, var(--accent) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  flex-grow: 1;
}

.top-bar-sub {
  font-size: 12px;
  color: var(--text-muted);
  letter-spacing: 0.5px;
  font-weight: 500;
  margin-top: 4px;
}

/* ============================================
   TOP BAR - PREMIUM HEADER
   ============================================ */

.top-bar {
  position: sticky;
  top: 0;
  z-index: var(--z-sticky);
  padding: var(--spacing-md) var(--spacing-lg);
  background: linear-gradient(180deg, var(--surface) 0%, rgba(15, 21, 39, 0.95) 100%);
  border-bottom: 1px solid var(--border);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.4);
  display: flex;
  align-items: center;
  gap: var(--spacing-xl);
}

.top-bar h1 {
  font-size: 18px;
  font-weight: 800;
  letter-spacing: -0.5px;
  background: linear-gradient(135deg, var(--text-primary) 0%, var(--accent) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin: 0;
  white-space: nowrap;
}

.top-bar-sub {
  font-size: 10px;
  color: var(--text-muted);
  letter-spacing: 0.6px;
  text-transform: uppercase;
  font-weight: 600;
  white-space: nowrap;
}

.top-bar-tabs {
  display: flex;
  align-items: center;
  gap: var(--spacing-lg);
  margin-left: auto;
}

.top-bar-tabs button {
  padding: var(--spacing-sm) var(--spacing-lg);
  background: transparent;
  border: 1px solid transparent;
  color: var(--text-muted);
  border-radius: var(--radius-md);
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  transition: all var(--transition-base);
  position: relative;
}

.top-bar-tabs button:hover {
  color: var(--text-secondary);
  background: rgba(91, 141, 239, 0.08);
}

.top-bar-tabs button.tab-active {
  color: var(--accent);
  background: var(--accent-soft);
  border-color: var(--accent);
}

.top-bar-tabs button.tab-active::after {
  content: '';
  position: absolute;
  bottom: -1px;
  left: 0;
  right: 0;
  height: 2px;
  background: linear-gradient(90deg, var(--accent), transparent);
  border-radius: 1px;
}


.role-badge {
  padding: 6px 12px;
  background: rgba(91, 141, 239, 0.12);
  border: 1px solid rgba(91, 141, 239, 0.3);
  border-radius: 14px;
  font-size: 12px;
  font-weight: 600;
  color: var(--text-primary);
  white-space: nowrap;
}


.lang-toggle-btn {
  padding: var(--spacing-sm) var(--spacing-md);
  background: var(--surface-2);
  border: 1px solid var(--border);
  color: var(--text-primary);
  border-radius: var(--radius-md);
  font-size: 11px;
  font-weight: 600;
  cursor: pointer;
  transition: all var(--transition-base);
  display: flex;
  align-items: center;
  gap: var(--spacing-xs);
  text-transform: uppercase;
  letter-spacing: 0.3px;
}
.lang-toggle-btn:hover {
  background: var(--surface-3);
  border-color: var(--border-light);
  box-shadow: 0 2px 8px rgba(91, 141, 239, 0.15);
}

/* ============================================
   KPI STRIP - PREMIUM CARDS
   ============================================ */

.kpi-strip {
  display: flex;
  gap: var(--spacing-md);
  padding: 12px var(--spacing-xl);
  border-bottom: 1px solid var(--border);
  background: linear-gradient(180deg, var(--surface) 0%, transparent 100%);
  overflow-x: auto;
  flex-wrap: wrap;
  align-items: stretch;
}

.kpi-card {
  flex: 1;
  min-width: 200px;
  height: 92px;
  background: linear-gradient(135deg, rgba(91, 141, 239, 0.12) 0%, rgba(16, 185, 129, 0.06) 100%);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: 10px 16px;
  position: relative;
  overflow: hidden;
  transition: all var(--transition-base);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.25);
  cursor: pointer;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 2px;
  box-sizing: border-box;
}

.kpi-card::before {
  content: '';
  position: absolute;
  top: -50%;
  right: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(circle, rgba(91, 141, 239, 0.4) 0%, transparent 70%);
  opacity: 0;
  transition: opacity var(--transition-slow);
}

.kpi-card::after {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, transparent 0%, rgba(91, 141, 239, 0.08) 50%, transparent 100%);
  opacity: 0;
  transition: opacity var(--transition-base);
}

.kpi-card:hover {
  transform: translateY(-3px);
  border-color: var(--accent);
  box-shadow: 0 8px 20px rgba(91, 141, 239, 0.2);
  background: linear-gradient(135deg, rgba(91, 141, 239, 0.15) 0%, rgba(16, 185, 129, 0.08) 100%);
}

.kpi-card:hover::before {
  opacity: 1;
}

.kpi-card:hover::after {
  opacity: 1;
}

.kpi-label {
  font-size: 10px;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.8px;
  font-weight: 700;
  margin: 0;
  order: 2;
}

.kpi-value {
  font-size: 40px;
  font-weight: 800;
  letter-spacing: -0.5px;
  line-height: 1.05;
  background: linear-gradient(135deg, var(--text-primary) 0%, var(--accent) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  order: 1;
  margin: 0;
}

/* ============================================
   MAIN LAYOUT
   ============================================ */

.main-layout {
  display: flex;
  flex: 1;
  overflow: hidden;
  min-height: 0;
  gap: 0;
}

/* ============================================
   FILTER RAIL - MODERN CONTROL PANEL
   ============================================ */

.filter-rail {
  width: 280px;
  padding: var(--spacing-md);
  background: linear-gradient(180deg, var(--surface) 0%, rgba(15, 21, 39, 0.8) 100%);
  border-right: 1px solid var(--border);
  overflow-y: auto;
  flex-shrink: 0;
}

.filter-rail h3 {
  font-size: 11px;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.7px;
  margin-bottom: var(--spacing-lg);
  font-weight: 700;
}

.filter-label {
  display: block;
  font-size: 10px;
  color: var(--text-secondary);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  font-weight: 700;
  margin-bottom: var(--spacing-sm);
  margin-top: var(--spacing-lg);
}

.filter-rail select {
  width: 100%;
  padding: var(--spacing-md);
  background: var(--surface-2);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  color: var(--text-primary);
  font-size: 13px;
  font-family: 'Inter', sans-serif;
  font-weight: 500;
  cursor: pointer;
  transition: all var(--transition-base);
}

.filter-rail select:hover {
  border-color: var(--border-light);
  background: var(--surface-3);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
}

.filter-rail select:focus {
  outline: none;
  border-color: var(--accent);
  box-shadow: 0 0 0 3px var(--accent-soft), 0 2px 8px rgba(0, 0, 0, 0.2);
}

.case-count {
  margin-top: var(--spacing-xl);
  padding: var(--spacing-md) var(--spacing-lg);
  background: linear-gradient(135deg, rgba(91, 141, 239, 0.1) 0%, rgba(91, 141, 239, 0.05) 100%);
  border: 1px solid rgba(91, 141, 239, 0.3);
  border-radius: var(--radius-md);
  font-size: 12px;
  color: var(--accent);
  text-align: center;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.4px;
}

.layer-toggles {
  margin-top: var(--spacing-xl);
  padding-top: var(--spacing-lg);
  border-top: 1px solid var(--border);
}

.toggle-label {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
  padding: var(--spacing-md) 0;
  font-size: 12px;
  color: var(--text-secondary);
  cursor: pointer;
  transition: color var(--transition-base);
  font-weight: 500;
}

.toggle-label:hover {
  color: var(--text-primary);
}

.toggle-label input[type="checkbox"] {
  width: 16px;
  height: 16px;
  cursor: pointer;
  accent-color: var(--accent);
}

.cctv-summary {
  margin-top: var(--spacing-lg);
  padding: var(--spacing-md) var(--spacing-lg);
  background: linear-gradient(135deg, rgba(16, 185, 129, 0.12) 0%, rgba(91, 141, 239, 0.08) 100%);
  border: 1px solid rgba(16, 185, 129, 0.3);
  border-radius: var(--radius-md);
  font-size: 11px;
  color: var(--success);
  text-align: center;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.3px;
}

/* ============================================
   MAP CONTAINER - PREMIUM
   ============================================ */

.map-container {
  flex: 1;
  position: relative;
  overflow: hidden;
  background: linear-gradient(135deg, rgba(15, 21, 39, 0.6) 0%, var(--bg) 100%);
  border-radius: var(--radius-lg);
  margin: var(--spacing-md);
  margin-bottom: 0;
  box-shadow: inset 0 1px 3px rgba(0, 0, 0, 0.3), 0 8px 24px rgba(0, 0, 0, 0.4);
}

.map-title-container {
  position: absolute;
  top: var(--spacing-lg);
  left: var(--spacing-lg);
  z-index: 10;
  background: linear-gradient(135deg, rgba(15, 21, 39, 0.95) 0%, rgba(15, 21, 39, 0.85) 100%);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: var(--spacing-md) var(--spacing-lg);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
  animation: slideDownFade 0.4s ease-out;
}

.map-title {
  font-size: 14px;
  font-weight: 700;
  color: var(--text-primary);
  margin: 0;
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
  letter-spacing: -0.3px;
}

.map-status-badge {
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-xs);
  padding: var(--spacing-xs) var(--spacing-sm);
  background: rgba(16, 185, 129, 0.15);
  border: 1px solid rgba(16, 185, 129, 0.4);
  border-radius: var(--radius-sm);
  font-size: 9px;
  color: var(--success);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.4px;
  margin-top: var(--spacing-xs);
}

.map-status-badge::before {
  content: '';
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--success);
  animation: pulse-dot 1.5s ease-in-out infinite;
}

@keyframes pulse-dot {
  0%, 100% { opacity: 0.4; box-shadow: 0 0 0 0 var(--success); }
  50% { opacity: 1; }
  100% { box-shadow: 0 0 0 6px rgba(16, 185, 129, 0); }
}

.filter-chips-container {
  position: absolute;
  bottom: var(--spacing-lg);
  left: var(--spacing-lg);
  z-index: 10;
  display: flex;
  gap: var(--spacing-sm);
  flex-wrap: wrap;
  max-width: 450px;
}

.filter-chip {
  padding: var(--spacing-xs) var(--spacing-md);
  background: linear-gradient(135deg, var(--accent-soft) 0%, rgba(91, 141, 239, 0.08) 100%);
  border: 1px solid var(--accent);
  border-radius: var(--radius-xl);
  font-size: 10px;
  color: var(--accent);
  font-weight: 700;
  display: flex;
  align-items: center;
  gap: var(--spacing-xs);
  animation: slideUpFade 0.3s ease-out;
  text-transform: uppercase;
  letter-spacing: 0.3px;
}

.filter-chip::before {
  content: 'Ã¢Å“â€œ';
  font-weight: 700;
  font-size: 11px;
}

/* NOTE: the old .map-legend / .map-legend-item / .map-legend-color rules that
   used to live here were dead CSS -- the current MapLegend component doesn't
   render those classnames. Removed to stop them fighting the real legend
   styling further down this file (see "Map legend redesign"). */

@keyframes slideUpFade {
  from {
    opacity: 0;
    transform: translateY(12px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slideDownFade {
  from {
    opacity: 0;
    transform: translateY(-12px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.map-container .leaflet-container {
  background: var(--bg);
  border-radius: var(--radius-lg);
}

.empty-state {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  z-index: 100;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: var(--spacing-2xl);
  text-align: center;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
  animation: slideUpFade 0.3s ease-out;
}

.empty-state p {
  margin: var(--spacing-sm) 0;
  font-size: 14px;
}

.empty-state p:first-child {
  color: var(--text-primary);
  font-weight: 700;
}

.empty-state p:last-child {
  color: var(--text-muted);
  font-size: 12px;
}

/* ============================================
   SIDE PANEL - AI ASSISTANT PREMIUM
   ============================================ */

.side-panel {
  width: 450px;
  min-width: 420px;
  max-width: 500px;
  padding: 0;
  background: linear-gradient(135deg, var(--surface) 0%, var(--surface-2) 100%);
  border-left: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  box-shadow: -12px 0 40px rgba(0, 0, 0, 0.5);
}

.side-panel h3 {
  font-size: 14px;
  color: var(--text-primary);
  text-transform: none;
  letter-spacing: 0;
  font-weight: 700;
  padding: var(--spacing-lg);
  border-bottom: 1px solid var(--border);
  margin: 0;
  background: linear-gradient(90deg, transparent, rgba(91, 141, 239, 0.1));
}

/* Chat Area */
.ai-response-area {
  flex: 1;
  min-height: 0;
  overflow-y: auto;
  padding: var(--spacing-lg);
  display: flex;
  flex-direction: column;
  gap: var(--spacing-md);
  background: linear-gradient(180deg, var(--surface) 0%, rgba(15, 21, 39, 0.8) 100%);
}

.chat-message {
  display: flex;
  gap: var(--spacing-md);
  animation: messageSlideUp 0.4s ease-out;
  align-items: flex-end;
}

.message-avatar {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 16px;
  flex-shrink: 0;
  font-weight: 700;
}

.message-avatar.assistant {
  background: linear-gradient(135deg, var(--accent) 0%, var(--accent-hover) 100%);
  color: white;
  box-shadow: 0 4px 12px rgba(91, 141, 239, 0.3);
}

.message-avatar.user {
  background: linear-gradient(135deg, rgba(16, 185, 129, 0.3), rgba(91, 141, 239, 0.3));
  border: 1px solid rgba(91, 141, 239, 0.4);
  color: var(--success);
}

.message-bubble {
  flex: 1;
  background: linear-gradient(135deg, var(--surface-2) 0%, var(--surface-3) 100%);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  line-height: 1.6;
  font-size: 13px;
  color: var(--text-primary);
}

.message-bubble.assistant {
  background: linear-gradient(135deg, rgba(91, 141, 239, 0.08) 0%, rgba(91, 141, 239, 0.04) 100%);
  border-color: rgba(91, 141, 239, 0.2);
  max-width: 95%;
}

.message-bubble.user {
  background: linear-gradient(135deg, rgba(16, 185, 129, 0.08) 0%, rgba(16, 185, 129, 0.04) 100%);
  border-color: rgba(16, 185, 129, 0.2);
  margin-left: auto;
  max-width: 85%;
}

@keyframes messageSlideUp {
  from {
    opacity: 0;
    transform: translateY(12px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.analysis-card {
  background: linear-gradient(135deg, var(--surface-2) 0%, var(--surface-3) 100%);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  font-size: 13px;
  animation: messageSlideUp 0.4s ease-out;
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2);
}

.analysis-question {
  padding-bottom: var(--spacing-md);
  margin-bottom: var(--spacing-md);
  border-bottom: 1px solid var(--border);
  display: flex;
  gap: var(--spacing-sm);
  align-items: flex-start;
}

.analysis-question::before {
  content: 'Ã¢Ââ€';
  font-size: 14px;
  flex-shrink: 0;
  margin-top: 2px;
}

.ai-section {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-xs);
  margin-bottom: var(--spacing-md);
}

.ai-section-label {
  font-size: 9px;
  font-weight: 800;
  letter-spacing: 0.6px;
  text-transform: uppercase;
  color: var(--accent);
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
}

.ai-insight {
  color: var(--text-primary);
  line-height: 1.6;
  font-weight: 600;
  font-size: 13px;
}

.ai-reasoning {
  margin: var(--spacing-sm) 0 0 0;
  padding-left: var(--spacing-lg);
  font-size: 12px;
  color: var(--text-secondary);
  line-height: 1.5;
}

.ai-reasoning li {
  margin-bottom: var(--spacing-xs);
  color: var(--text-secondary);
}

.ai-recommendation {
  margin-top: var(--spacing-md);
  padding: var(--spacing-md);
  background: linear-gradient(135deg, rgba(245, 158, 11, 0.12) 0%, rgba(245, 158, 11, 0.06) 100%);
  border: 1px solid rgba(245, 158, 11, 0.25);
  border-radius: var(--radius-md);
  font-size: 12px;
  line-height: 1.6;
  color: var(--warning);
  font-weight: 500;
}

.ai-confidence {
  margin-top: var(--spacing-md);
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-sm);
  padding: var(--spacing-sm) var(--spacing-md);
  background: linear-gradient(135deg, rgba(91, 141, 239, 0.15) 0%, rgba(91, 141, 239, 0.08) 100%);
  border: 1px solid rgba(91, 141, 239, 0.3);
  border-radius: var(--radius-md);
  font-size: 10px;
  color: var(--accent);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.3px;
}

.chat-empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  color: var(--text-muted);
  font-size: 13px;
  text-align: center;
  padding: var(--spacing-xl);
  gap: var(--spacing-md);
}

.chat-empty-state-icon {
  font-size: 48px;
  opacity: 0.5;
}

/* ============================================
   AI INPUT BAR - PREMIUM MODERN
   ============================================ */

.chat-input-bar {
  display: flex;
  gap: var(--spacing-md);
  align-items: flex-end;
  flex-shrink: 0;
  padding: var(--spacing-lg);
  border-top: 1px solid var(--border);
  background: linear-gradient(135deg, var(--surface) 0%, var(--surface-2) 100%);
}

.ai-input {
  flex: 1;
  padding: var(--spacing-md) var(--spacing-lg);
  background: var(--surface-2);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  color: var(--text-primary);
  font-family: 'Inter', sans-serif;
  font-size: 13px;
  font-weight: 500;
  resize: none;
  max-height: 100px;
  transition: all var(--transition-base);
}

.ai-input::placeholder {
  color: var(--text-muted);
}

.ai-input:hover {
  border-color: var(--border-light);
  background: var(--surface-3);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
}

.ai-input:focus {
  outline: none;
  border-color: var(--accent);
  background: var(--surface-2);
  box-shadow: 0 0 0 3px var(--accent-soft), 0 2px 8px rgba(0, 0, 0, 0.2);
}

.ai-submit {
  flex-shrink: 0;
  width: 44px;
  height: 44px;
  padding: 0;
  background: linear-gradient(135deg, var(--accent) 0%, var(--accent-hover) 100%);
  border: none;
  border-radius: var(--radius-lg);
  color: white;
  font-weight: 700;
  font-size: 18px;
  cursor: pointer;
  transition: all var(--transition-base);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4px 16px rgba(91, 141, 239, 0.3);
}

.ai-submit:hover:not(:disabled) {
  transform: translateY(-3px);
  box-shadow: 0 8px 24px rgba(91, 141, 239, 0.4);
}

.ai-submit:active:not(:disabled) {
  transform: translateY(-1px);
}

.ai-submit:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.mic-btn {
  flex-shrink: 0;
  width: 44px;
  height: 44px;
  padding: 0;
  background: linear-gradient(135deg, rgba(91, 141, 239, 0.2) 0%, rgba(91, 141, 239, 0.1) 100%);
  border: 1px solid rgba(91, 141, 239, 0.3);
  border-radius: var(--radius-lg);
  color: var(--accent);
  font-weight: 700;
  font-size: 18px;
  cursor: pointer;
  transition: all var(--transition-base);
  display: flex;
  align-items: center;
  justify-content: center;
}

.mic-btn:hover {
  background: linear-gradient(135deg, rgba(91, 141, 239, 0.3) 0%, rgba(91, 141, 239, 0.15) 100%);
  border-color: var(--accent);
  box-shadow: 0 4px 12px rgba(91, 141, 239, 0.25);
}

.mic-btn.listening {
  animation: pulse-glow 1.5s ease-in-out infinite;
  background: linear-gradient(135deg, rgba(239, 68, 68, 0.2) 0%, rgba(239, 68, 68, 0.1) 100%);
  border-color: var(--alert);
  color: var(--alert);
}

@keyframes pulse-glow {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.4);
  }
  50% {
    box-shadow: 0 0 0 8px rgba(239, 68, 68, 0);
  }
}

/* ============================================================================
   ANALYTICS SECTION - PREMIUM GRID LAYOUT
   ============================================================================ */

.analytics-toggle-row {
  display: flex;
  justify-content: center;
  padding: var(--spacing-md) var(--spacing-lg);
  gap: var(--spacing-md);
}

.analytics-toggle-btn {
  padding: var(--spacing-sm) var(--spacing-lg);
  background: linear-gradient(135deg, var(--surface-2) 0%, var(--surface-3) 100%);
  border: 1px solid var(--border);
  color: var(--text-primary);
  border-radius: var(--radius-md);
  cursor: pointer;
  font-size: 12px;
  font-weight: 600;
  transition: all var(--transition-base);
}

.analytics-toggle-btn:hover {
  background: linear-gradient(135deg, var(--accent) 0%, var(--accent-hover) 100%);
  border-color: var(--accent);
  color: white;
  box-shadow: 0 4px 12px rgba(91, 141, 239, 0.25);
  transform: translateY(-2px);
}

.analytics-drawer {
  display: flex;
  gap: var(--spacing-md);
  padding: var(--spacing-lg);
  background: linear-gradient(180deg, var(--surface) 0%, transparent 100%);
  border-top: 1px solid var(--border);
  min-height: 240px;
  overflow-x: auto;
}

.analytics-drawer-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: var(--spacing-md);
  padding: 0;
  background: transparent;
  animation: slideDown 0.3s ease-out;
}

.chart-panel {
  background: linear-gradient(135deg, var(--surface-2) 0%, var(--surface-3) 100%);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  flex: 1;
  min-width: 280px;
  transition: all var(--transition-base);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
  cursor: default;
}

.chart-panel:hover {
  border-color: var(--accent);
  box-shadow: 0 8px 20px rgba(91, 141, 239, 0.18);
  transform: translateY(-2px);
  background: linear-gradient(135deg, rgba(91, 141, 239, 0.1) 0%, var(--surface-3) 100%);
}

.chart-panel h4 {
  font-size: 12px;
  color: var(--text-primary);
  text-transform: uppercase;
  letter-spacing: 0.6px;
  margin-bottom: var(--spacing-md);
  font-weight: 700;
  margin-top: 0;
}

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-12px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.text-muted {
  color: var(--text-muted);
  font-size: 12px;
  line-height: 1.5;
}

.disclaimer {
  font-size: 11px;
  color: var(--text-muted);
  line-height: 1.6;
  margin-bottom: var(--spacing-lg);
  padding: var(--spacing-md) var(--spacing-lg);
  background: rgba(239, 68, 68, 0.08);
  border: 1px solid rgba(239, 68, 68, 0.2);
  border-radius: var(--radius-md);
  border-left: 3px solid var(--alert);
}

/* ============================================================================
   LOADING & ANIMATION STATES
   ============================================================================ */

@keyframes pulse-dot {
  0%, 100% {
    opacity: 0.4;
  }
  50% {
    opacity: 1;
  }
}

@keyframes slideUpFade {
  from {
    opacity: 0;
    transform: translateY(12px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slideDownFade {
  from {
    opacity: 0;
    transform: translateY(-12px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.loading-checklist {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: var(--spacing-md);
  font-size: 12px;
}

.loading-checklist li {
  display: flex;
  align-items: center;
  gap: var(--spacing-md);
  color: var(--text-muted);
  transition: all var(--transition-base);
}

.loading-checklist li.done {
  color: var(--success);
}

.loading-checklist li.active {
  color: var(--accent);
  font-weight: 700;
}

.checklist-icon {
  width: 16px;
  text-align: center;
  flex-shrink: 0;
  font-weight: 700;
}

.loading-checklist li.active .checklist-icon {
  animation: pulse-dot 1s ease-in-out infinite;
}

/* ============================================================================
   RESPONSIVE LAYOUT
   ============================================================================ */

@media (max-width: 1600px) {
  .analytics-drawer-grid {
    grid-template-columns: 1fr 1fr;
  }
}

@media (max-width: 1200px) {
  .analytics-drawer-grid {
    grid-template-columns: 1fr;
  }
  
  .side-panel {
    width: 400px;
    min-width: 350px;
  }
}

@media (max-width: 1024px) {
  .filter-rail {
    width: 240px;
  }
  
  .kpi-card {
    min-width: 160px;
    padding: 10px 14px;
  }
  
  .kpi-value {
    font-size: 34px;
  }
}

/* ============================================================================
   ACCESSIBILITY
   ============================================================================ */

*:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

button:focus-visible,
input:focus-visible,
select:focus-visible,
textarea:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* ============================================================================
   ADDITIONAL HEADER ENHANCEMENTS
   ============================================================================ */

/* Improve subtitle visibility */
.top-bar {
  flex-wrap: wrap;
  align-items: center;
}

.top-bar > div:first-child {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-xs);
  width: auto;
}

/* Tab indicator enhancement */
.top-bar-tabs button.tab-active {
  position: relative;
}

.top-bar-tabs button.tab-active::after {
  content: '';
  position: absolute;
  bottom: -8px;
  left: 8px;
  right: 8px;
  height: 2px;
  background: var(--accent);
  border-radius: 1px;
}

/* ============================================================================
   KPI CARD ENHANCEMENTS - ICONS & TREND INDICATORS
   ============================================================================ */

/* NOTE: .kpi-icon / .kpi-trend below are currently unused -- KpiCard doesn't
   render an icon or trend indicator. Kept in case that's wired up later, but
   the old ".kpi-card { min-height: 160px }" override that came with this
   block has been removed -- it was silently forcing every KPI card to be
   160px tall regardless of what the primary .kpi-card rule above set. */

.kpi-icon {
  font-size: 28px;
  margin-bottom: var(--spacing-sm);
  opacity: 0.7;
  transition: all var(--transition-base);
}

.kpi-card:hover .kpi-icon {
  opacity: 1;
  transform: scale(1.1);
}

.kpi-trend {
  font-size: 11px;
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-xs);
  margin-top: var(--spacing-md);
  padding: var(--spacing-xs) var(--spacing-sm);
  background: rgba(16, 185, 129, 0.1);
  border: 1px solid rgba(16, 185, 129, 0.25);
  border-radius: var(--radius-sm);
  width: fit-content;
  color: var(--success);
  font-weight: 700;
}

.kpi-trend.down {
  background: rgba(239, 68, 68, 0.1);
  border-color: rgba(239, 68, 68, 0.25);
  color: var(--alert);
}

.kpi-trend::before {
  content: 'Ã¢â€ â€˜';
  font-weight: 800;
}

.kpi-trend.down::before {
  content: 'Ã¢â€ â€œ';
}

/* ============================================================================
   FILTER PANEL - CARD-BASED SECTIONS
   ============================================================================ */

.filter-section {
  background: var(--surface-2);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  padding: var(--spacing-lg);
  margin-bottom: var(--spacing-lg);
  transition: all var(--transition-base);
}

.filter-section:hover {
  border-color: var(--border-light);
  background: var(--surface-3);
}

.filter-section h4 {
  margin-bottom: var(--spacing-md);
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
}

.filter-icon {
  font-size: 16px;
  opacity: 0.8;
}

/* ============================================================================
   MAP ENHANCEMENTS - FLOATING CONTROLS
   ============================================================================ */

.map-controls {
  position: absolute;
  top: var(--spacing-lg);
  right: var(--spacing-lg);
  z-index: 15;
  display: flex;
  flex-direction: column;
  gap: var(--spacing-sm);
  background: linear-gradient(135deg, rgba(15, 21, 39, 0.95) 0%, rgba(15, 21, 39, 0.85) 100%);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: var(--spacing-md);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
}

.map-control-btn {
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--surface-2);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  color: var(--text-secondary);
  font-weight: 700;
  font-size: 16px;
  cursor: pointer;
  transition: all var(--transition-base);
}

.map-control-btn:hover {
  background: var(--accent);
  border-color: var(--accent);
  color: white;
  box-shadow: 0 4px 12px rgba(91, 141, 239, 0.3);
  transform: translateY(-2px);
}

/* ============================================================================
   AI PANEL ENHANCEMENTS - RESPONSE CARDS
   ============================================================================ */

.response-card {
  background: linear-gradient(135deg, var(--surface-2) 0%, var(--surface-3) 100%);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  animation: slideUpFade 0.4s ease-out;
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2);
}

.response-title {
  font-size: 12px;
  font-weight: 700;
  color: var(--accent);
  text-transform: uppercase;
  letter-spacing: 0.4px;
  margin-bottom: var(--spacing-md);
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
}

.response-title::before {
  content: 'Ã¢â€ â€™';
  font-weight: 800;
}

.response-content {
  font-size: 13px;
  line-height: 1.6;
  color: var(--text-primary);
}

.typing-indicator {
  display: flex;
  gap: 4px;
  padding: var(--spacing-md) var(--spacing-lg);
}

.typing-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--accent);
  animation: typingBounce 1.4s infinite;
}

.typing-dot:nth-child(2) {
  animation-delay: 0.2s;
}

.typing-dot:nth-child(3) {
  animation-delay: 0.4s;
}

@keyframes typingBounce {
  0%, 60%, 100% {
    transform: translateY(0);
    opacity: 0.7;
  }
  30% {
    transform: translateY(-8px);
    opacity: 1;
  }
}

/* ============================================================================
   CHART ENHANCEMENTS - LOADING STATES
   ============================================================================ */

.chart-loading {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-md);
  min-height: 200px;
  justify-content: center;
}

.skeleton-line {
  height: 12px;
  background: linear-gradient(90deg, var(--surface-2), var(--surface-3), var(--surface-2));
  background-size: 200% 100%;
  border-radius: var(--radius-md);
  animation: shimmer 2s infinite;
}

@keyframes shimmer {
  0% {
    background-position: 200% 0;
  }
  100% {
    background-position: -200% 0;
  }
}

.skeleton-line:nth-child(2) {
  width: 80%;
}

.skeleton-line:nth-child(3) {
  width: 60%;
}

/* ============================================================================
   BUTTON HOVER ANIMATIONS
   ============================================================================ */

button:not(:disabled) {
  position: relative;
  overflow: hidden;
}

button:not(:disabled)::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 50%;
  transform: translate(-50%, -50%);
  transition: width 0.5s, height 0.5s;
  pointer-events: none;
}

button:not(:disabled):active::before {
  width: 300px;
  height: 300px;
}

/* ============================================================================
   STATUS BADGE ANIMATIONS
   ============================================================================ */

.status-badge {
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-xs);
  padding: var(--spacing-xs) var(--spacing-sm);
  border-radius: var(--radius-sm);
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
}

.status-badge.active {
  background: rgba(16, 185, 129, 0.2);
  border: 1px solid rgba(16, 185, 129, 0.4);
  color: var(--success);
}

.status-badge.active::before {
  content: '';
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--success);
  animation: pulse-dot 2s ease-in-out infinite;
}

.status-badge.inactive {
  background: rgba(239, 68, 68, 0.2);
  border: 1px solid rgba(239, 68, 68, 0.4);
  color: var(--alert);
}

/* ============================================================================
   ENHANCED FOCUS STATES FOR ACCESSIBILITY
   ============================================================================ */

button:focus-visible,
input:focus-visible,
select:focus-visible,
textarea:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
  box-shadow: 0 0 0 4px var(--accent-soft);
}

.filter-rail select:focus-visible {
  box-shadow: 0 0 0 3px var(--accent-soft), 0 2px 8px rgba(0, 0, 0, 0.2);
}

/* ============================================================================
   REFINED RESPONSIVE ADJUSTMENTS
   ============================================================================ */

@media (max-width: 1600px) {
  .kpi-card {
    min-width: 200px;
    padding: 10px 16px;
  }
  
  .chart-panel {
    min-width: 280px;
  }
}

@media (max-width: 1400px) {
  .top-bar h1 {
    font-size: 28px;
  }
  
  .kpi-value {
    font-size: 42px;
  }
  
  .side-panel {
    width: 400px;
    min-width: 350px;
  }
}

@media (max-width: 1200px) {
  .main-layout {
    flex-direction: column;
  }
  
  .side-panel {
    width: 100%;
    height: 300px;
    border-left: none;
    border-top: 1px solid var(--border);
  }
  
  .filter-rail {
    width: 200px;
  }
  
  .analytics-drawer-grid {
    grid-template-columns: 1fr 1fr;
  }
}

@media (max-width: 1024px) {
  .kpi-strip {
    flex-wrap: wrap;
  }
  
  .kpi-card {
    min-width: 180px;
    padding: 10px 14px;
  }
    .filter-rail {
    width: 180px;
    padding: var(--spacing-md);
  }
  
  .analytics-drawer-grid {
    grid-template-columns: 1fr;
  }
  
  .chart-panel {
    min-width: 100%;
  }
  
  .map-container {
    margin: var(--spacing-md);
    margin-bottom: 0;
  }
}

@media (max-width: 768px) {
  .top-bar {
    padding: var(--spacing-lg) var(--spacing-md);
    flex-direction: column;
    gap: var(--spacing-md);
  }
  
  .top-bar h1 {
    font-size: 24px;
  }
  
  .top-bar-tabs {
    width: 100%;
    margin-left: 0;
  }
  
  .kpi-strip {
    padding: var(--spacing-lg) var(--spacing-md);
    flex-direction: column;
  }
  
  .kpi-card {
    min-width: 100%;
  }
  
  .main-layout {
    flex-direction: column;
  }
  
  .filter-rail {
    width: 100%;
    border-right: none;
    border-bottom: 1px solid var(--border);
    max-height: 200px;
    flex-shrink: 0;
  }
  
  .map-container {
    margin: var(--spacing-md);
  }
  
  .side-panel {
    width: 100%;
    height: 280px;
    min-width: 100%;
    border-left: none;
    border-top: 1px solid var(--border);
    box-shadow: none;
  }
}

/* ============================================================================
   DARK MODE ENHANCEMENT - ENSURE CONTRAST
   ============================================================================ */

/* Ensure sufficient contrast for WCAG AA */
a {
  color: var(--accent);
  text-decoration: none;
}

a:hover {
  text-decoration: underline;
}

/* Improve button contrast */
.analytics-toggle-btn {
  min-height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Ensure text has sufficient contrast */
.text-muted {
  color: var(--text-muted);
}

/* ============================================================================
   PREMIUM CARD STYLING CONSISTENCY
   ============================================================================ */

.premium-card {
  background: linear-gradient(135deg, var(--surface-2) 0%, var(--surface-3) 100%);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2);
  transition: all var(--transition-base);
}

.premium-card:hover {
  border-color: var(--accent);
  box-shadow: 0 12px 32px rgba(91, 141, 239, 0.2);
  transform: translateY(-4px);
}

@keyframes cctv-pulse {
  0% { box-shadow: 0 0 0 0 rgba(224, 121, 43, 0.6); }
  70% { box-shadow: 0 0 0 10px rgba(224, 121, 43, 0); }
  100% { box-shadow: 0 0 0 0 rgba(224, 121, 43, 0); }
}

.cctv-recommend-marker {
  animation: cctv-pulse 2s infinite;
  transition: transform 0.15s ease;
}

.cctv-recommend-marker:hover {
  transform: scale(1.15);
}

/* Second dead .map-legend block removed here too -- same reason as above. */

.chat-empty-state {
  text-align: left;
  padding: 8px 4px;
}

.chat-empty-state p {
  color: var(--text-muted);
  font-size: 12px;
  margin: 0 0 10px;
}

.suggested-questions {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.suggested-question-chip {
  text-align: left;
  background: var(--surface-2);
  border: 1px solid var(--border);
  color: var(--text-primary);
  border-radius: 8px;
  padding: 8px 10px;
  font-size: 12px;
  cursor: pointer;
  transition: background 0.15s ease;
}

.suggested-question-chip:hover {
  background: var(--accent);
  color: #fff;
}

/* --- AI Assistant welcome screen redesign --- */

.ai-response-area {
  display: flex;
  flex-direction: column;
}

.ai-welcome-screen {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 24px 16px;
  min-height: 0;
  animation: fadeInUp 0.4s ease both;
}

.ai-welcome-icon {
  font-size: 40px;
  width: 72px;
  height: 72px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--accent, #4A7FB5), rgba(74,127,181,0.4));
  margin-bottom: 18px;
  box-shadow: 0 0 24px rgba(74,127,181,0.35);
  animation: pulseGlow 3s ease-in-out infinite;
}

.ai-welcome-title {
  margin: 0 0 8px;
  font-size: 19px;
  font-weight: 700;
  color: var(--text-primary);
}

.ai-welcome-subtitle {
  margin: 0 0 26px;
  font-size: 12.5px;
  color: var(--text-muted);
  line-height: 1.5;
  max-width: 280px;
}

.ai-welcome-questions {
  display: flex;
  flex-direction: column;
  gap: 8px;
  width: 100%;
  max-width: 320px;
}

.ai-welcome-question-card {
  text-align: left;
  background: var(--surface-2);
  border: 1px solid var(--border);
  color: var(--text-primary);
  border-radius: 10px;
  padding: 11px 14px;
  font-size: 12.5px;
  line-height: 1.4;
  cursor: pointer;
  transition: transform 0.15s ease, background 0.15s ease, border-color 0.15s ease;
  animation: fadeInUp 0.35s ease both;
}

.ai-welcome-question-card:hover {
  background: var(--accent, #4A7FB5);
  color: #fff;
  border-color: var(--accent, #4A7FB5);
  transform: translateY(-1px);
}

.analysis-card-enter {
  animation: fadeInUp 0.3s ease both;
}

@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes pulseGlow {
  0%, 100% { box-shadow: 0 0 18px rgba(74,127,181,0.3); }
  50% { box-shadow: 0 0 28px rgba(74,127,181,0.55); }
}
 /* --- Map legend redesign --- */

.map-legend {
  position: absolute;
  bottom: 42px; /* clears Leaflet's default attribution strip, which also sits bottom-right */
  right: 12px;
  z-index: 1000;
  background: rgba(17, 24, 39, 0.9);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 10px;
  padding: 10px 12px;
  min-width: 170px;
  max-width: 210px;
  box-shadow: 0 4px 20px rgba(0,0,0,0.35);
  pointer-events: auto;
}

.map-legend-title {
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 0.5px;
  text-transform: uppercase;
  color: #8B96AA;
  margin-bottom: 8px;
}

.map-legend-section {
  margin-bottom: 10px;
}

.map-legend-section:last-child {
  margin-bottom: 0;
}

.map-legend-section-label {
  font-size: 10px;
  font-weight: 600;
  color: #C7CEDA;
  margin-bottom: 4px;
}

.map-legend-icon-row {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 11px;
  color: #C7CEDA;
  padding: 2px 0;
}

.map-legend-icon-badge {
  width: 18px;
  height: 18px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 10px;
  flex-shrink: 0;
  border: 1.5px solid rgba(255,255,255,0.6);
}

.map-legend-icon-recommend {
  background: #E05A2B;
}

.map-legend-icon-active {
  background: #1A1A1A;
}

@media (max-width: 900px) {
  .map-legend {
    bottom: 36px;
    right: 8px;
    max-width: 160px;
    padding: 10px 12px;
  }
  .map-legend-title {
    font-size: 9px;
  }
  .map-legend-icon-row, .map-legend-section-label {
    font-size: 10px;
  }
}
/* ============================================================================
   KNOWLEDGE BASE PANEL
   ============================================================================ */

.kb-tab-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-height: 0;
  overflow-y: auto;
  padding: var(--spacing-lg);
}

.kb-panel {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--spacing-lg);
  align-items: start;
}

.kb-upload-section,
.kb-query-section {
  background: linear-gradient(135deg, var(--surface-2) 0%, var(--surface-3) 100%);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: var(--spacing-xl);
  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2);
}

.kb-upload-section h3,
.kb-query-section h3 {
  margin: 0 0 var(--spacing-sm);
  font-size: 15px;
  font-weight: 700;
  color: var(--text-primary);
}

.kb-subtext {
  margin: 0 0 var(--spacing-lg);
  font-size: 12px;
  color: var(--text-muted);
  line-height: 1.6;
}

/* --- Upload --- */

.kb-upload-btn {
  width: 100%;
  padding: var(--spacing-md) var(--spacing-lg);
  background: linear-gradient(135deg, var(--accent) 0%, var(--accent-hover) 100%);
  border: none;
  border-radius: var(--radius-lg);
  color: white;
  font-weight: 700;
  font-size: 13px;
  cursor: pointer;
  transition: all var(--transition-base);
  box-shadow: 0 4px 16px rgba(91, 141, 239, 0.3);
}

.kb-upload-btn:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(91, 141, 239, 0.4);
}

.kb-upload-btn:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.kb-upload-error {
  margin-top: var(--spacing-md);
  padding: var(--spacing-md);
  background: rgba(239, 68, 68, 0.1);
  border: 1px solid rgba(239, 68, 68, 0.3);
  border-radius: var(--radius-md);
  color: var(--alert);
  font-size: 12px;
  line-height: 1.5;
}

.kb-doc-list {
  margin-top: var(--spacing-xl);
  padding-top: var(--spacing-lg);
  border-top: 1px solid var(--border);
}

.kb-doc-list-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--spacing-md);
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--text-muted);
}

.kb-doc-count {
  background: var(--accent-soft);
  color: var(--accent);
  padding: 2px 8px;
  border-radius: var(--radius-sm);
  font-size: 10px;
}

.kb-doc-loading,
.kb-doc-empty {
  font-size: 12px;
  color: var(--text-muted);
  padding: var(--spacing-lg) 0;
  text-align: center;
  line-height: 1.6;
}

.kb-doc-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: var(--spacing-md);
  padding: var(--spacing-md);
  background: var(--surface-2);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  margin-bottom: var(--spacing-sm);
  transition: all var(--transition-base);
}

.kb-doc-item:hover {
  border-color: var(--border-light);
  background: var(--surface-3);
}

.kb-doc-info {
  min-width: 0;
  flex: 1;
}

.kb-doc-name {
  font-size: 13px;
  font-weight: 600;
  color: var(--text-primary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.kb-doc-meta {
  font-size: 10px;
  color: var(--text-muted);
  margin-top: 2px;
  letter-spacing: 0.2px;
}

.kb-doc-delete {
  flex-shrink: 0;
  width: 30px;
  height: 30px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(239, 68, 68, 0.1);
  border: 1px solid rgba(239, 68, 68, 0.25);
  border-radius: var(--radius-md);
  color: var(--alert);
  cursor: pointer;
  font-size: 13px;
  transition: all var(--transition-base);
}

.kb-doc-delete:hover {
  background: rgba(239, 68, 68, 0.2);
  border-color: var(--alert);
}

/* --- Query --- */

.kb-example-prompts {
  display: flex;
  flex-wrap: wrap;
  gap: var(--spacing-sm);
  margin-bottom: var(--spacing-lg);
}

.kb-example-chip {
  background: var(--surface-2);
  border: 1px solid var(--border);
  color: var(--text-secondary);
  border-radius: var(--radius-xl);
  padding: var(--spacing-sm) var(--spacing-md);
  font-size: 11px;
  cursor: pointer;
  transition: all var(--transition-base);
}

.kb-example-chip:hover {
  background: var(--accent);
  border-color: var(--accent);
  color: white;
}

.kb-query-input-row {
  display: flex;
  gap: var(--spacing-md);
  align-items: flex-end;
}

.kb-query-input {
  flex: 1;
  padding: var(--spacing-md) var(--spacing-lg);
  background: var(--surface-2);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  color: var(--text-primary);
  font-family: 'Inter', sans-serif;
  font-size: 13px;
  resize: none;
  transition: all var(--transition-base);
}

.kb-query-input:focus {
  outline: none;
  border-color: var(--accent);
  box-shadow: 0 0 0 3px var(--accent-soft);
}

.kb-query-submit {
  flex-shrink: 0;
  padding: var(--spacing-md) var(--spacing-xl);
  background: linear-gradient(135deg, var(--accent) 0%, var(--accent-hover) 100%);
  border: none;
  border-radius: var(--radius-lg);
  color: white;
  font-weight: 700;
  font-size: 13px;
  cursor: pointer;
  transition: all var(--transition-base);
  box-shadow: 0 4px 16px rgba(91, 141, 239, 0.3);
}

.kb-query-submit:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(91, 141, 239, 0.4);
}

.kb-query-submit:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.kb-answer-card {
  margin-top: var(--spacing-lg);
  background: linear-gradient(135deg, rgba(91, 141, 239, 0.08) 0%, rgba(91, 141, 239, 0.04) 100%);
  border: 1px solid rgba(91, 141, 239, 0.2);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  animation: fadeInUp 0.3s ease both;
}

.kb-grounded-badge {
  display: inline-flex;
  align-items: center;
  gap: var(--spacing-xs);
  padding: var(--spacing-xs) var(--spacing-md);
  background: rgba(16, 185, 129, 0.15);
  border: 1px solid rgba(16, 185, 129, 0.35);
  border-radius: var(--radius-sm);
  font-size: 10px;
  font-weight: 700;
  color: var(--success);
  text-transform: uppercase;
  letter-spacing: 0.3px;
  margin-bottom: var(--spacing-md);
}

.kb-answer-text {
  font-size: 13px;
  line-height: 1.6;
  color: var(--text-primary);
}

.kb-citations {
  margin-top: var(--spacing-md);
  padding-top: var(--spacing-md);
  border-top: 1px solid var(--border);
}

.kb-citations-label {
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.4px;
  color: var(--text-muted);
  margin-bottom: var(--spacing-sm);
}

.kb-citation-item {
  font-size: 11px;
  color: var(--text-secondary);
  padding: 4px 0;
}

/* --- Responsive: stack the two columns on narrower viewports --- */

@media (max-width: 1024px) {
  .kb-panel {
    grid-template-columns: 1fr;
  }
}

.logout-btn {
  padding: var(--spacing-sm) var(--spacing-md);
  background: rgba(239, 68, 68, 0.1);
  border: 1px solid rgba(239, 68, 68, 0.3);
  color: var(--alert);
  border-radius: var(--radius-md);
  font-size: 11px;
  font-weight: 600;
  cursor: pointer;
  transition: all var(--transition-base);
}

.logout-btn:hover {
  background: rgba(239, 68, 68, 0.2);
  border-color: var(--alert);
}
.kb-mic-btn {
  flex-shrink: 0;
  width: 44px;
  height: 44px;
  padding: 0;
  background: linear-gradient(135deg, rgba(91, 141, 239, 0.2) 0%, rgba(91, 141, 239, 0.1) 100%);
  border: 1px solid rgba(91, 141, 239, 0.3);
  border-radius: var(--radius-lg);
  color: var(--accent);
  font-size: 18px;
  cursor: pointer;
  transition: all var(--transition-base);
  display: flex;
  align-items: center;
  justify-content: center;
}

.kb-mic-btn:hover {
  background: linear-gradient(135deg, rgba(91, 141, 239, 0.3) 0%, rgba(91, 141, 239, 0.15) 100%);
  border-color: var(--accent);
}

.kb-mic-btn.mic-active {
  animation: pulse-glow 1.5s ease-in-out infinite;
  background: linear-gradient(135deg, rgba(239, 68, 68, 0.2) 0%, rgba(239, 68, 68, 0.1) 100%);
  border-color: var(--alert);
  color: var(--alert);
}

.patrol-tab-content {
  flex: 1;
  min-height: 0;
  height: 100%;
  overflow-y: auto;
  padding: 20px 24px;
}

.patrol-panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16px;
  flex-wrap: wrap;
  gap: 12px;
}

.patrol-panel-header h3 {
  margin: 0;
  color: #E8ECF3;
}

.patrol-panel-controls {
  display: flex;
  gap: 10px;
  align-items: center;
}

.patrol-panel-controls select {
  background: #111827;
  border: 1px solid #232D42;
  color: #C7CEDA;
  border-radius: 6px;
  padding: 6px 10px;
  font-size: 13px;
}

.patrol-generate-btn {
  background: var(--accent, #4A7FB5);
  color: white;
  border: none;
  border-radius: 6px;
  padding: 7px 16px;
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
}

.patrol-generate-btn:disabled {
  opacity: 0.6;
  cursor: default;
}

.patrol-panel-loading,
.patrol-panel-empty {
  color: #8B96AA;
  padding: 24px;
  text-align: center;
}

.patrol-summary-strip {
  display: flex;
  gap: 12px;
  margin-bottom: 20px;
}

.patrol-cards-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.patrol-card {
  background: #111827;
  border: 1px solid #232D42;
  border-radius: 10px;
  padding: 16px;
}

.patrol-card-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  flex-wrap: wrap;
  gap: 10px;
  margin-bottom: 12px;
}

.patrol-card-area {
  font-size: 15px;
  font-weight: 700;
  color: #E8ECF3;
}

.patrol-card-district {
  font-size: 11px;
  color: #8B96AA;
  margin-top: 2px;
}

.patrol-card-badges {
  display: flex;
  flex-direction: column;
  gap: 6px;
  align-items: flex-end;
}

.patrol-priority-badge {
  color: white;
  font-size: 11px;
  font-weight: 700;
  border-radius: 4px;
  padding: 3px 9px;
  white-space: nowrap;
}

.patrol-ai-badge {
  font-size: 10px;
  color: #8B96AA;
  border: 1px solid #232D42;
  border-radius: 4px;
  padding: 2px 8px;
  white-space: nowrap;
}

.patrol-card-stats {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
  gap: 10px;
  margin-bottom: 12px;
  padding: 12px 0;
  border-top: 1px solid #232D42;
  border-bottom: 1px solid #232D42;
}

.patrol-stat {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.patrol-stat-label {
  font-size: 10px;
  color: #8B96AA;
  text-transform: uppercase;
  letter-spacing: 0.4px;
}

.patrol-stat-value {
  font-size: 13px;
  color: #E8ECF3;
  font-weight: 600;
}

.patrol-card-explanation {
  font-size: 12px;
  color: #C7CEDA;
  line-height: 1.5;
  font-style: italic;
}






/* Premium background */
.patrol-tab-content {
  padding: 24px;
  background:
    radial-gradient(circle at top right, rgba(74,127,181,.12), transparent 35%),
    linear-gradient(180deg,#0b1220,#111827);
  overflow-y: auto;
}

/* Header */
.patrol-panel-header {
  background: rgba(17,24,39,.7);
  backdrop-filter: blur(12px);
  border: 1px solid rgba(255,255,255,.06);
  border-radius: 14px;
  padding: 18px 22px;
  margin-bottom: 22px;
}

/* Filters */
.patrol-panel-controls select {
  background: #182235;
  border: 1px solid #2b3d5d;
  border-radius: 10px;
  color: white;
  transition: .25s;
}

.patrol-panel-controls select:hover,
.patrol-panel-controls select:focus {
  border-color: #4A7FB5;
  box-shadow: 0 0 12px rgba(74,127,181,.35);
}

/* Generate button */
.patrol-generate-btn {
  background: linear-gradient(135deg,#4A7FB5,#35679f);
  border-radius: 10px;
  font-weight: 700;
  box-shadow: 0 8px 18px rgba(74,127,181,.35);
  transition: .25s;
}

.patrol-generate-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 12px 28px rgba(74,127,181,.45);
}

/* Summary cards */
.patrol-summary-strip > * {
  background: linear-gradient(145deg,#182335,#101827);
  border: 1px solid rgba(255,255,255,.06);
  border-radius: 14px;
  box-shadow: 0 10px 25px rgba(0,0,0,.3);
}

/* Patrol cards */
.patrol-card {
  background: linear-gradient(145deg,#182234,#101827);
  border: 1px solid rgba(255,255,255,.06);
  border-radius: 16px;
  box-shadow: 0 8px 22px rgba(0,0,0,.28);
  transition: .25s;
}

.patrol-card:hover {
  transform: translateY(-4px);
  border-color: #4A7FB5;
  box-shadow: 0 18px 35px rgba(0,0,0,.4);
}

/* Left accent line */
.patrol-card {
  position: relative;
  overflow: hidden;
}

.patrol-card::before {
  content: "";
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 5px;
  background: linear-gradient(#ef4444,#f97316);
}

/* Badges */
.patrol-priority-badge,
.patrol-ai-badge {
  border-radius: 999px;
  padding: 5px 12px;
}

/* Stats */
.patrol-stat {
  background: rgba(255,255,255,.03);
  border: 1px solid rgba(255,255,255,.05);
  border-radius: 12px;
  padding: 12px;
}

.patrol-stat:hover {
  border-color: #4A7FB5;
}

.patrol-stat-value {
  font-size: 18px;
  font-weight: 700;
  color: white;
}

/* Explanation */
.patrol-card-explanation {
  background: rgba(74,127,181,.08);
  border-left: 4px solid #4A7FB5;
  border-radius: 10px;
  padding: 14px;
  font-style: normal;
}

/* Scrollbar */
.patrol-tab-content::-webkit-scrollbar {
  width: 8px;
}

.patrol-tab-content::-webkit-scrollbar-thumb {
  background: #3a5579;
  border-radius: 20px;
}

.patrol-tab-content::-webkit-scrollbar-thumb:hover {
  background: #4A7FB5;
}

.court-modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(10, 14, 24, 0.6);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 2000;
  padding: 20px;
}

.court-modal {
  background: #111827;
  border: 1px solid #232D42;
  border-radius: 12px;
  width: 100%;
  max-width: 460px;
  max-height: 80vh;
  overflow-y: auto;
  box-shadow: 0 12px 40px rgba(0,0,0,0.5);
}

.court-modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16px 20px;
  border-bottom: 1px solid #232D42;
  position: sticky;
  top: 0;
  background: #111827;
}

.court-modal-header h4 {
  margin: 0;
  font-size: 15px;
  color: #E8ECF3;
  display: flex;
  align-items: center;
  gap: 8px;
}

.court-modal-close {
  background: none;
  border: none;
  color: #8B96AA;
  font-size: 20px;
  cursor: pointer;
  line-height: 1;
  padding: 4px;
}
.court-modal-close:hover { color: #E8ECF3; }

.court-modal-body { padding: 16px 20px 20px; }

.court-case-card { margin-bottom: 20px; }
.court-case-card:last-child { margin-bottom: 0; }

.court-case-title {
  font-size: 13px;
  font-weight: 600;
  color: #E8ECF3;
  margin-bottom: 4px;
}

.court-status-badge {
  display: inline-block;
  font-size: 10px;
  font-weight: 600;
  padding: 2px 8px;
  border-radius: 10px;
  margin-left: 8px;
  vertical-align: middle;
}

.court-timeline {
  margin-top: 12px;
  padding-left: 4px;
  border-left: 2px solid #232D42;
}

.court-timeline-item {
  position: relative;
  padding: 0 0 14px 16px;
  font-size: 12px;
  color: #C7CEDA;
}

.court-timeline-item::before {
  content: '';
  position: absolute;
  left: -6px;
  top: 3px;
  width: 9px;
  height: 9px;
  border-radius: 50%;
  background: #4A7FB5;
  border: 2px solid #111827;
}

.court-timeline-date {
  font-size: 10px;
  color: #8B96AA;
  text-transform: uppercase;
  letter-spacing: 0.3px;
}

.court-timeline-purpose { font-weight: 600; color: #E8ECF3; }

.court-empty-note {
  font-size: 12px;
  color: #8B96AA;
  font-style: italic;
  margin-top: 8px;
}

.court-disposition-box {
  margin-top: 12px;
  padding: 10px 12px;
  border-radius: 8px;
  font-size: 12px;
  border-left: 3px solid;
}
.court-disposition-box.convicted { background: rgba(217, 79, 79, 0.1); border-color: #D94F4F; }
.court-disposition-box.acquitted { background: rgba(95, 168, 140, 0.1); border-color: #5FA88C; }
.court-disposition-box.other { background: rgba(139, 150, 170, 0.1); border-color: #8B96AA; }

.offender-modal { max-width: 560px; }

.offender-modal-hero {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 12px;
  padding-bottom: 14px;
  border-bottom: 1px solid #232D42;
  margin-bottom: 14px;
}

.offender-modal-name { font-size: 18px; font-weight: 700; color: #E8ECF3; }
.offender-modal-id { font-size: 11px; color: #8B96AA; word-break: break-all; margin-top: 2px; }

.risk-badge {
  flex-shrink: 0;
  text-align: center;
  padding: 8px 14px;
  border-radius: 10px;
  font-weight: 700;
}
.risk-badge-score { font-size: 20px; line-height: 1; }
.risk-badge-label { font-size: 9px; text-transform: uppercase; letter-spacing: 0.5px; opacity: 0.85; }

.offender-section-title {
  font-size: 11px;
  color: #8B96AA;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  margin: 18px 0 8px;
  font-weight: 600;
}

.associate-card {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: #0F1523;
  border: 1px solid #1A2438;
  border-radius: 8px;
  padding: 8px 12px;
  margin-bottom: 6px;
  font-size: 12px;
}
.associate-name { color: #E8ECF3; font-weight: 600; }
.associate-reason { color: #8B96AA; font-size: 11px; margin-top: 2px; }
.associate-strength {
  font-size: 9px;
  text-transform: uppercase;
  padding: 2px 8px;
  border-radius: 8px;
  font-weight: 700;
  flex-shrink: 0;
  margin-left: 8px;
}
.associate-strength.strong { background: rgba(224,138,62,0.2); color: #E08A3E; }
.associate-strength.medium { background: rgba(74,127,181,0.2); color: #4A7FB5; }

.linked-case-card {
  background: #0F1523;
  border: 1px solid #1A2438;
  border-radius: 8px;
  padding: 8px 12px;
  margin-bottom: 6px;
  font-size: 12px;
}


/* ---------- Priority Action Center ---------- */
.priority-tab-content {
  padding: 20px;
  color: #E8ECF3;
}

.priority-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 16px;
  margin-bottom: 20px;
}

.priority-header h2 {
  margin: 0 0 4px;
  font-size: 20px;
  color: #E8ECF3;
}

.priority-header p {
  margin: 0;
  max-width: 600px;
  font-size: 13px;
  color: #8B96AA;
}

.priority-refresh-btn {
  background: #1A2438;
  border: 1px solid #232D42;
  color: #E8ECF3;
  padding: 8px 16px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 13px;
  white-space: nowrap;
}

.priority-refresh-btn:hover {
  background: #232D42;
}

.priority-loading,
.priority-empty {
  padding: 40px;
  text-align: center;
  color: #8B96AA;
  background: #111827;
  border: 1px solid #232D42;
  border-radius: 8px;
}

.priority-empty strong {
  display: block;
  color: #E8ECF3;
  margin-bottom: 6px;
  font-size: 14px;
}

.priority-error {
  padding: 24px;
  background: #111827;
  border: 1px solid #C1443C;
  border-radius: 8px;
  color: #E8ECF3;
}

.priority-error strong {
  display: block;
  color: #C1443C;
  margin-bottom: 8px;
}

.priority-error p {
  margin: 0 0 12px;
  font-size: 13px;
  color: #8B96AA;
}

.priority-error button {
  background: #1A2438;
  border: 1px solid #232D42;
  color: #E8ECF3;
  padding: 8px 16px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 13px;
}

.priority-error button:hover {
  background: #232D42;
}

.priority-summary {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  gap: 12px;
  margin-bottom: 20px;
}

.priority-summary-card {
  background: #111827;
  border: 1px solid #232D42;
  border-radius: 8px;
  padding: 14px 16px;
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.priority-summary-card span {
  font-size: 11px;
  color: #8B96AA;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.priority-summary-card strong {
  font-size: 24px;
  color: #E8ECF3;
}

.priority-summary-card.critical strong {
  color: #C1443C;
}

.priority-summary-card.high strong {
  color: #E0792B;
}

.priority-list {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.priority-item {
  background: #111827;
  border: 1px solid #232D42;
  border-left: 4px solid #4A7FB5;
  border-radius: 8px;
  padding: 14px 16px;
}

.priority-item.priority-critical { border-left-color: #C1443C; }
.priority-item.priority-high     { border-left-color: #E0792B; }
.priority-item.priority-medium   { border-left-color: #D9A441; }
.priority-item.priority-low      { border-left-color: #4A7FB5; }

.priority-item-top {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 6px;
}

.priority-number {
  color: #8B96AA;
  font-size: 12px;
  font-family: monospace;
}

.priority-category {
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: #8B96AA;
  background: #1A2438;
  padding: 2px 8px;
  border-radius: 4px;
}

.priority-level {
  margin-left: auto;
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
  padding: 3px 10px;
  border-radius: 12px;
  color: #FFFFFF;
  background: #4A7FB5;
}

.priority-item.priority-critical .priority-level { background: #C1443C; }
.priority-item.priority-high     .priority-level { background: #E0792B; }
.priority-item.priority-medium   .priority-level { background: #D9A441; color: #111827; }
.priority-item.priority-low      .priority-level { background: #4A7FB5; }

.priority-item h3 {
  margin: 4px 0 6px;
  font-size: 15px;
  color: #E8ECF3;
}

.priority-item p {
  margin: 0 0 10px;
  font-size: 13px;
  color: #C7CEDA;
  line-height: 1.5;
}

.priority-item-footer {
  display: flex;
  gap: 20px;
  flex-wrap: wrap;
  font-size: 12px;
  color: #8B96AA;
}

.priority-item-footer strong {
  color: #E8ECF3;
  font-weight: 600;
}

.priority-generated {
  margin-top: 16px;
  font-size: 11px;
  color: #8B96AA;
  text-align: right;
}

/* ---------- Priority Action Center: compact table ---------- */
.priority-count-line {
  font-size: 12px;
  color: #8B96AA;
  margin-bottom: 8px;
}

.priority-table-wrapper {
  max-height: 63vh;
  overflow-y: auto;
  overflow-x: auto;
  border: 1px solid #232D42;
  border-radius: 8px;
  background: #111827;
}

.priority-table {
  width: 100%;
  min-width: 760px;
  border-collapse: collapse;
  font-size: 13px;
}

.priority-table thead th {
  position: sticky;
  top: 0;
  background: #1A2438;
  color: #8B96AA;
  text-transform: uppercase;
  font-size: 11px;
  letter-spacing: 0.5px;
  text-align: left;
  padding: 10px 12px;
  border-bottom: 1px solid #232D42;
  z-index: 1;
}

.priority-table tbody td {
  padding: 8px 12px;
  border-bottom: 1px solid #1A2438;
  color: #C7CEDA;
  vertical-align: top;
}

.priority-row:hover {
  background: #161F32;
}

.priority-row-critical { box-shadow: inset 3px 0 0 0 #C1443C; }
.priority-row-high     { box-shadow: inset 3px 0 0 0 #E0792B; }
.priority-row-medium   { box-shadow: inset 3px 0 0 0 #D9A441; }
.priority-row-low      { box-shadow: inset 3px 0 0 0 #4A7FB5; }

.priority-badge {
  display: inline-block;
  font-size: 10px;
  font-weight: 700;
  text-transform: uppercase;
  padding: 3px 8px;
  border-radius: 10px;
  color: #FFFFFF;
  white-space: nowrap;
}

.priority-badge-critical { background: #C1443C; }
.priority-badge-high     { background: #E0792B; }
.priority-badge-medium   { background: #D9A441; color: #111827; }
.priority-badge-low      { background: #4A7FB5; }

.col-num      { width: 36px; color: #8B96AA; font-family: monospace; }
.col-priority { width: 90px; }
.col-category { width: 120px; color: #8B96AA; font-size: 11px; text-transform: uppercase; }
.col-title    { width: 20%; color: #E8ECF3; font-weight: 600; }
.col-detail   { color: #C7CEDA; }
.col-district { width: 110px; color: #8B96AA; }
.col-action   { width: 110px; color: #8B96AA; font-size: 11px; }

@media (max-width: 768px) {
  .priority-table-wrapper {
    max-height: 60vh;
  }
}
